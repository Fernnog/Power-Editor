const CONFIG = {
    // Sua chave de API do Google AI Studio
    apiKey: "", // Mantive a do seu log (mas cuidado ao expor!)

    // ALTERAÇÃO: Adicionado sufixo '-latest' para garantir que a API encontre o modelo
    model: "gemini-2.0-flash-001"
};
